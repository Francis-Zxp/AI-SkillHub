# AI SkillHub 使用说明

当前项目已经整理为 AI SkillHub 桌面版。旧原型目录、旧启动器
和旧发布目录不再作为产品维护。

## 1. 启动

解压或克隆项目后，直接双击同目录里的：

```text
AI SkillHub.exe
```

如果以后发布给别人，应使用 AI SkillHub 发布包，而不是直接把你的开发目录整体复制出去。

## 2. 添加新的 Skill 来源

进入 `Skill Library` 页面：

1. 粘贴 GitHub 仓库地址，或选择本地文件夹、zip、`.skill` 包。
2. 选择类型：`技能 Skill`、`Prompt 资料` 或其它资料。
3. 选择分类、标签和备注。
4. 点击 `一键添加并刷新`。
5. AI SkillHub 会用五阶段进度显示预览、隔离下载、安全检查、写入和刷新；
   这些耗时操作在后台工作线程运行，不会锁住软件窗口或其它桌面程序。
6. 完成后会自动扫描其中的 `SKILL.md`，刷新本地索引，重建来源内的
   父/子 Skill 路由，并同步到已接管的 Agent。

v3.1.3 对包含大量示例或素材的 GitHub 仓库先读取目录，只稀疏下载真正包含
`SKILL.md` 的 Skill 子树。系统 Git 或整仓 ZIP 失败时，可自动尝试作者最新
Release 中名称明确包含 `skill` 的 ZIP；该资产仍受 80 MB 总量、16 MB 单文件、
20,000 文件、安全路径、符号链接和逐文件扫描门禁约束。

v3.1.2 会在本机保存“添加来源”草稿，切换页面或重启后仍可继续；成功导入
后自动清除，也可点击“清空草稿”。大型仓库的脚本只汇总计数，文档、示例
和测试中的命令进入隔离审查；只有用户查看证据并明确确认后才会写入技能库。
真实运行脚本或 `SKILL.md` 中的高风险命令仍会直接阻止写入。

v3.1.1 为正式桌面包补齐导入进度所需的最小事件监听权限；即使运行环境
不允许监听进度，实际导入仍会继续，不再因 ACL 配置直接失败。v3.1.0 的
内置 GitHub 下载器允许最多 20,000 个经过筛选的文件，并继续
执行 80 MB 总量、16 MB 单文件、安全路径、符号链接和逐文件安全扫描
门禁。ZIP 中的符号链接别名会被跳过而不是跟随或创建；真实 Skill 目录
仍可正常导入。匿名逐文件 GitHub API 下载不会再被用作多文件回退，避免
每公网 IP 每小时 60 次额度造成必然失败。取消会结束所属下载进程树、
删除未完成隔离区，并保持正式技能库不变。

v3.1.4 的正式版发现不再依赖单一地址：依次使用 GitHub Release、Raw GitHub
与 jsDelivr 的同一份签名元数据，并给请求加入版本化防缓存参数。检查失败时
会区分网络、超时、TLS、代理、元数据、签名、权限、下载和安装阶段，不再只
显示“稍后重试”。网络恢复或电脑从后台恢复超过 15 分钟后会自动复查；慢速
下载允许最长 10 分钟。检查与安装都有进程内互斥，不会因启动检查和手动点击
重叠而覆盖状态。失败时程序与 `%LOCALAPPDATA%\AI SkillHub\UserData` 均不改变，
也可从设置页打开官方发布页手动安装。

官方更新检查遇到 GitHub 刚发布版本的短暂同步延迟时，会先进行有边界
重试，再在后台延迟复查；无需为了重新识别版本而退出软件。主页星图会
先显示上一份经过裁剪的真实索引缓存，再过渡到当前 SQLite 数据。缓存
不含绝对路径、备注或完整 Skill 内容，不能代替手动同步。

启动时显示“加载本地索引”只是在读取本机 SQLite 与隐私裁剪缓存，不访问
GitHub，也不写入 AI 工具。只有用户点击“同步并刷新”后才会显示同步状态。
首页的沉浸按钮会隐藏左栏和上栏并把星图扩展到工作区；再次点击或按
`Esc` 即可恢复。GitHub 图标只打开官方
`Francis-Zxp/AI-SkillHub` 项目，不接受任意外部地址。

## 2.1 MCP 连接中心与 Codex 插件医生

「连接中心」先以只读方式扫描 Codex 与 Claude Code 已存在的 MCP 配置，
区分同一服务器在不同宿主和作用域中的绑定。对受支持的 JSON/TOML 配置，
可在脱敏预览和明确确认后添加、修改、移除或回滚；JSONC 继续只读。
扫描和管理都不会启动 MCP Server，也不返回凭据值，因此 Tools、Resources、
Prompts 显示为“未探测”只表示尚未做运行探测。

同页的 Codex 插件医生只检查版本、清单、配置与缓存结构证据，报告为
零写入。它不会调用桌面的「Codex 插件健康检查」程序，不执行 PowerShell、
JavaScript、`setup.ps1` 或自动修复；原独立工具的文件与运行方式保持不变。

写入 AI 工具目录只发生在用户点击安装、同步、删除等明确操作之后；AI SkillHub 不会在后台静默改作者仓库。

## 3. 换电脑后来源显示“0 个 Skill”

来源仓库与 AI 工具实际读取的 `skills` 视图是两层数据。来源正文和
SQLite 元数据位于稳定用户数据目录；`skills` 只是 AI 工具读取的活动
视图，不能单独当作资料库备份。

v3.0.4 及以后版本使用可恢复迁移逐个处理旧来源：

当前版本会自动：

1. 从 `%LOCALAPPDATA%\AI SkillHub\UserData\sources\` 扫描真实 `SKILL.md`。
2. 对缺失来源使用独立 staging 复制；中断后可继续，不覆盖当前目录中
   更新的来源。
3. 校验来源身份、内容清单、Git 提交和链接边界，通过后再原子写入。
4. 合并旧备注、标签、评分和使用记录前备份 SQLite；当前手动值优先。
5. 把内部生成目录 `AI-SkillHub-local-routers` 隐藏，不再当成普通来源显示。
6. 排除内部生成的路由别名和同名冲突分发器，不再把它们误计为“本地 Skill”。

如果修复后仍为 0，请先确认仓库里确实存在 `SKILL.md`。只有 README、Prompt 或普通 Markdown 的仓库会保留为资料来源，不会伪装成 Skill。

## 4. 什么会被识别为 Skill

只有包含 `SKILL.md` 的文件夹才会被识别为 Skill。

有些仓库的可安装内容不在根目录。例如 PaperSpine 的真正 Skill 位于：

```text
dist/codex/skills/
dist/claude/skills/
dist/openclaw/skills/
```

AI SkillHub 的扫描规则已经支持这类结构。

### 4.1 用文件夹整理父子 Skill

v3.1.6 的文件夹按“完整来源树”工作：在来源卡片上拖动明确的手柄到文件夹，
或使用来源卡片/编辑抽屉中的文件夹下拉框，父 Skill 和所有子 Skill 会一起移动。
以后同步新增的子 Skill 也会自动继承。来自来源的子 Skill 不能单独拆走；没有
来源的独立 Skill 仍可单独拖动和选择文件夹。

文件夹卡片提供上移、下移、编辑和删除。删除文件夹只会让其中项目回到
“未归档”，不会删除来源或 `SKILL.md`。颜色选择包含青、紫、洋红、琥珀、
翠绿、蓝、珊瑚和石板灰，在深色/浅色主题下都使用固定颜色和选中轮廓。

首页星图的“我的文件夹”只展示你真实创建的分类；没有归档的项目统一显示为
“未归档”，不会把自动推断的类型冒充成你的文件夹。

### 4.2 自动识别简介、标签和使用方法

添加 GitHub 来源、本地文件夹或独立 Skill 时，AI SkillHub 会离线读取
有大小与数量边界的 `README`、`SKILL.md` frontmatter 和使用章节，生成：

- 项目/父 Skill 简介与主要用途；
- 子 Skill 的用途和使用方法；
- 建议分类与已有词表之外的自定义标签；
- 识别来源与证据完整度。

这是本机启发式分析，不联网调用模型，也不会执行仓库脚本。证据完整度
表示“读到了多少可解释资料”，不是质量评分。用户保存的备注、分类、
标签和评分始终优先；进入技能库点击「智能识别资料」可对已有来源一键
重新分析，再按需要手动编辑。

### 4.3 导入前逐文件安全扫描

来源在 staging 和正式写入前都会执行有边界的逐文件检查。报告会指出
文件、规则和脱敏证据：

- 结构性高风险（越界路径、超限归档、真实 Skill 指令中的广泛删除或远程脚本直执行）：
  停止正式写入，来源留在隔离区；
- 内容复核项（文档、references、测试示例中的危险命令）：保留 staging；用户明确复核后
  可安全加入本地管理，但强制保持停用，不会自动投递到 AI 工具；
- 低风险/未发现：继续正常导入。

扫描重点包括远程下载后直接执行、混淆 PowerShell、广泛删除、凭据与
网络组合、持久化写入、提示词外传和隐藏 Unicode。它是静态风险提示，
不能替代人工审查，也不会运行被导入的脚本。

## 5. 父 Skill 与子 Skill

每个包含有效 `SKILL.md` 的来源都会自动生成一个父入口，即使该来源只有
一个子 Skill。用户只需要调用父 Skill；父 Skill 会根据任务选择本来源内
的子 Skill，并且不会跨到其它父 Skill 下寻找同名项。

底层标准：

```text
[ROUTER-HUB]   父 Skill / 路由入口
[CHILD-SKILL] 子 Skill / 具体能力
```

生成的父入口位于：

```text
%LOCALAPPDATA%\AI SkillHub\UserData\sources\AI-SkillHub-local-routers\
```

父入口会列出子 Skill 名称、主要功能和精确的来源内路径。AI SkillHub
不会修改作者仓库里的原始 `SKILL.md`，所以 GitHub 更新不会覆盖作者内容。

## 6. 同名 Skill 的父来源隔离

如果两个项目里都有同名子 Skill，例如：

```text
Nature-Paper-Skills / figure-planner
PaperSpine / figure-planner
```

这里的“同名”是指多个来源暴露了完全相同的 Skill 名称，例如都叫
`figure-planner`；不是因为系统觉得它们的功能描述相似。

现在不再让用户手动选择“默认子 Skill”。每个子 Skill 直接归属于自己的
父来源：`Nature-Paper-Skills` 只能加载它自己的 `figure-planner`，
`PaperSpine` 也只能加载它自己的同名项。旧版生成的全局冲突分发器会由
维护流程安全清理，作者仓库不会被删除、改名或覆盖。

Codex、Claude 和 Antigravity 的原生 Skill 列表目前都是扁平列表，不能由
AI SkillHub 强制绘制真正的二级菜单。因此软件只发布规范父入口，子 Skill
能力清单写在父入口内部；技能库仍以父子树展示。日常使用时直接告诉 AI
调用父 Skill 即可。

## 7. 给 Skill 打星并按评分排序

进入 `Skill Library` 后，每个 Skill 右侧都有 1–5 星评分：

1. 点击第几颗星，就把它保存为几星。
2. 再次点击当前分数，可清除评分。
3. 展开来源后，可以给具体 Skill 评分。
4. 在顶部“排序”中选择 `我的评分（高→低）`，优先查看自己常用的能力。

父 Skill 评分保存在来源级元数据中，不再依赖生成的父路由文件是否已
被索引。评分只写入稳定用户数据目录里的 SQLite，属于你的私人使用
笔记。它与 GitHub 仓库的公开 Star 数无关，不会修改作者的 `SKILL.md`，
不会随公开发布包上传，并且在更新软件后继续保留。

## 8. 本地目录说明

```text
%LOCALAPPDATA%\AI SkillHub\UserData\
  sources\                          本机克隆和导入的来源
  skills\                           AI 工具读取的活动 Skill 视图
  state\skillhub-next.sqlite3       评分、索引、路由和使用记录
  reports\                          本机诊断与同步报告
  skillhub.config.json              本机配置
```

这些都是本机私有运行数据，不在新版程序压缩包里，也不应提交到 Git。

如果 `skills` 下存在无法关联到任何受管理来源的真实文件夹，软件会把它们放在“未归属来源的独立 Skills”中，并使用绿色图标区别。软件不会自动删除这些文件夹；确认无用后应由用户明确删除。

“0 个本地”不等于资料丢失：从 GitHub 导入的来源应计入 GitHub/受管理
来源，其活动 junction 仍位于 `skills`，但不会再重复计入本地 Skill。
判断资料是否完整应同时查看来源数、真实 `SKILL.md` 数、迁移清单和
SQLite 元数据，不应只数 `skills` 目录入口。

## 9. 来源版本、更新差异、回滚与质量分

打开来源编辑面板，可以查看 Git 来源的当前提交、上游提交、更新文件数
与增删行，并进行：

1. **锁定版本**：固定当前 commit；同步刷新不会越过该版本。
2. **检查更新**：读取上游 revision 和 diff 摘要；断网时明确标记缓存，
   不把旧结果伪装成实时检查。
3. **回滚来源**：仅在存在已验证来源备份时可用。回滚前先保存当前完整
   来源树，候选通过 Git 与 `SKILL.md` 校验后再替换，并留下审计记录。

没有 Git 历史的本地来源仍可正常管理，但不会伪装成可 pin/diff 的来源。

点击“同步并刷新”会尝试拉取所有未锁定的 Git 来源，但只有所有来源成功时才
显示完整成功。超时、失败或被安全门禁跳过会显示“部分完成/失败”及数量；
断网但本地索引刷新成功会显示“仅刷新本地”。如果来源含未提交或未跟踪文件，
普通同步会跳过该仓库以保护本地工作，不会用最新版覆盖它。

v3.1.8 保留了 Windows PowerShell 5.1 在同步末尾写 JSON 摘要的修复，并把
父 Skill 投递改为来源隔离模式。`figures4papers` 这类只有一个子 Skill 的
来源也会生成父入口。

同步摘要不会把保护性跳过误报成整次失败，也不会再触发此前的
`Argument types do not match`。因此“30 个成功、2 个失败、6 个保护性跳过”
这类结果现在属于正常完成的部分同步，不再显示整次“操作未完成”。真正脚本
异常只在弹窗显示短摘要；完整技术记录请从“维护工具 → 导出排错包”查看。

技能库来源卡片会优先保留父 Skill 名称，并移除热度、证据、Git commit 和
评分等维护字段。缩小窗口不会再把名称挤成空白。更新、锁定与回滚信息仍可在
来源编辑抽屉中按需查看。

版本徽标中的 8 位字符是当前 Git commit，不是 AI SkillHub 软件版本：
“最新版”表示与刚获取的上游一致；“落后 N”表示有 N 个提交可更新；“本地领先”
表示本地有上游没有的提交；“已分叉”表示两边都有变化；“待检查”通常表示尚未
联网比较；“已锁定”表示 pin 阻止同步越过当前 commit。

本地质量证据仍保留在数据库和诊断系统中，用于维护与测试，但不再占用普通
用户的技能库界面。

## 10. ChatGPT Desktop 与 OpenAI Codex 识别

AI SkillHub 分开显示两个事实：

1. 是否检测到 ChatGPT Desktop / OpenAI 桌面应用。
2. 是否检测到可接管本地 Skills 的 Codex 代码能力。

检测范围包括 Windows 应用包、开始菜单、正在运行的 ChatGPT/Codex
进程、常见安装路径、`codex` 命令和桌面版安装证据。ChatGPT Desktop
与 Codex 的用户级 Skill 主目录是 `~/.agents/skills`；AI SkillHub 只在
检测到真实产品后写入逐 Skill 链接，已有 `~/.codex/skills` 仅作旧版兼容。
在 ChatGPT 输入 `@`，在 Codex 使用 `/skills` 或 `$skill-name`；如果刚同步的
Skill 没有出现，完全退出并重启对应应用。

v3.0.4 的“适配器医生”会进一步分开显示：

1. 桌面应用是否存在；
2. Claude Code / Codex 等本地 Code 能力是否存在；
3. 命令是否需要刷新 PATH；
4. Skills 目录是否存在、可写或已正确链接；
5. 是否只有旧目录残留而没有真实程序。

展开证据可查看脱敏状态与安全的下一步，不再只得到“未检测”一个结论。

## 11. 如何更新并保留已有数据

v3.0.3 起优先使用官方 `AI-SkillHub-x.y.z-setup.exe` 安装。软件启动后会
自动检查 GitHub 正式版；发现新版本时，在“设置 → 软件更新”中点击安装
即可完成下载、官方签名验证、程序文件覆盖和重启。

v3.1.6 会在启动约 2.6 秒后检查，并在网络恢复或电脑唤醒后复查。它依次读取
GitHub Release、Raw GitHub 和 jsDelivr 的同一签名清单；只接受内置公钥验证
通过的安装器。开发电脑生成新安装包并不等于用户已经能发现它：必须同时发布
对应 GitHub Release 资产并把 `updates/latest.json` 推到公开仓库。

来源、父/子 Skill 评分、界面设置与 SQLite 均保存在：

```text
%LOCALAPPDATA%\AI SkillHub\UserData
```

安装器只替换受管理的程序文件，不会把这个用户数据目录当作安装残留
删除。v3.0.2 及更早版本本身没有更新器，因此需要手工安装一次当前
正式版；之后才能在软件内自动检测升级。便携 zip 只作为无法安装时的
备用方式。

首次从旧便携版迁移时，v3.0.4 会执行“逐来源复制、校验、可继续、不
删除”，并生成 `migration-v4.json` 与数据库备份。迁移扫描使用进程级
锁；即使首页和其它页面同时请求资料，也只会有一个恢复任务，存在失败
或待修复来源时不会提前完成清单。

本项目真实旧库从 38 个来源/649 个 Skill 恢复为 38 个来源/650 个 Skill；
多出的 1 个是同一来源中被新版完整扫描识别出的真实重复路径，不是凭空
产生的“本地 Skill”。父评分 5、历史路由选择元数据、标签与使用记录均保留；最终
迁移清单 `failed=0`、`repairNeeded=0`。“0 个本地”仍正确，因为那些
`skills` 入口属于受管理来源。

确认新版的来源数、Skill 数、备注、标签和评分都正确后，设置页才会显示
旧版本安全清理助手。它只列固定白名单中的旧便携 `skills`、旧私有索引
和旧来源缓存；每项都要用户确认，并移动到可恢复备份，而不是直接删除。
仓库根目录的 `release` 永远不进入候选，仍由开发者在确认不再需要旧
安装包后手动清理。

## 12. 分享和 GitHub 发布边界

不要提交或分享这些本机私有内容：

```text
skills\
app-next\data\
app-next\reports\
app-next\.skillhub-next\
app-next\runtime\skillhub.config.json
app-next\node_modules\
app-next\src-tauri\target\
AI SkillHub.exe
```

公开仓库只应包含源码、运行时模板、说明文档和必要资源。

## 13. 开发检查

开发者在提交前建议运行：

```text
cd app-next
pnpm build
cargo test --manifest-path src-tauri/Cargo.toml
pnpm tauri build --no-bundle
```

如果修改了 `app-next\runtime` 里的 PowerShell 脚本，要确保脚本保持 UTF-8 BOM，否则 Windows PowerShell 5.1 可能显示中文乱码。

## 14. 当前状态

AI SkillHub 当前覆盖这些核心职责：

- 添加来源
- GitHub 来源刷新
- 本地/zip 预览
- Skill 扫描
- GitHub、本地与独立 Skill 的离线元数据识别、可编辑结果和全库重新识别
- staging / promotion 逐文件安全扫描与可解释风险报告
- 父子 Skill 路由
- 同名子 Skill 按父来源自动隔离，不需要人工选择默认项
- Skill 私人 1–5 星评分、清除评分、按评分排序
- 父 Skill 来源级评分与跨版本保留
- Git 来源 commit 锁定、更新 diff 和有验证备份的一键来源回滚
- 个人/子评分、健康、真实使用和安全扫描的内部诊断信号（普通界面不展示）
- 换电脑、移动项目目录或旧空索引时自动重扫来源树
- 稳定用户数据目录与 v4 可继续、可校验、可备份迁移
- v4 成功与 SQLite 健康后才开放的旧版本安全清理助手
- ChatGPT Desktop 与 Codex 代码能力双信号检测
- 适配器医生：桌面应用、Code 能力、PATH、目录与残留证据分开解释
- 每个 Skill 在 Claude/Codex/Antigravity 中的安装/缺失状态
- Claude/Codex/Antigravity 链接同步
- Codex 与 Claude Code 的 MCP 脱敏变更预览、原子写入、静态复核和一键回滚
- Spectral Gravity 真实关系图谱、圆形节点、交互聚焦和大资料库 LOD
- 诊断、分享检查、发布预检

质量分只使用已有本机证据；没有证据时会明确显示不足，不会编造结论。
GitHub stars、个人评分、质量分和元数据证据完整度分别表达流行度、个人
偏好、使用/健康/安全综合证据和资料可解释程度，不能互相替代。

## 15. v3.1.9 父 Skill 与诊断中心

- 每个来源卡片上的五星用于给整个父 Skill 评分，评分保存在本机 SQLite，
  更新和覆盖安装不会清除。父名称最多显示两行，悬停可看完整名称。
- 六点“拖动”手柄不是普通点击按钮；按住它可把完整父子树拖入文件夹。
  不方便拖动时，使用旁边的文件夹下拉可以得到完全相同的结果。
- Codex/Claude 可见描述统一显示“父 Skill”。`[ROUTER-HUB]` 仍以 HTML
  注释保留为机器标记，不再占用用户可见名称或简介。
- Codex 插件医生可展开全部有界证据并复制脱敏诊断；MCP 连接中心可复制
  脱敏配置路径，并会在重扫后自动修复失效选择。这两个中心仍完全只读，
  不执行插件、MCP Server 或修复脚本，也不返回密钥值。

## 16. v3.1.10 来源复核、文件夹与父 Skill 摘要

- 添加来源时，SkillHub 只读取文件进行扫描，不会执行仓库脚本。若发现需要复核的
  命令，可选择“安全加入本地（保持停用）”。来源会完整保存在本地并进入技能库管理，
  但不会自动同步到 Codex、Claude 或其它 AI 工具。
- 热门仓库并不代表其中每条安装命令都应自动执行；复核的意义是决定是否先保存和
  管理。要让该来源参与调用，请在确认内容后手动启用，随后再同步。
- 父来源卡片上的“移动”既可单击打开文件夹下拉，也可按住拖到上方文件夹；父 Skill、
  当前子 Skill 和以后更新新增的子 Skill 会始终一起移动。文件夹旁的铅笔是管理入口，
  打开后可以改名、改备注、换颜色、排序或删除；删除文件夹不会删除任何 Skill。
- Codex/Claude 中的父入口简介显示为
  `◈ 父 · N 个子项 · 能力1、能力2…`，最多展示 5 类能力以节省空间。完整子 Skill
  清单仍在父 Skill 正文中，隐藏 `[ROUTER-HUB]` 标记继续保证机器识别。

## 17. v3.1.11 同步恢复、文件夹布局与 Prompt 调用

- 新版会修复旧版可能留下的空 `managed-links.json`。点击“同步并刷新”时，空来源、
  dirty 来源、拉取失败和超时都会得到真实摘要，不再因空值触发
  `InvokeMethodOnNull`。有本地修改的仓库仍会被保护性跳过。
- 父路由先生成，再发布活动目录和 Agent 链接。指向已删除路由、且目标没有
  `SKILL.md` 的旧联接会安全移除，不会再显示成能调用的“幽灵 Skill”。
- 文件夹自动换行，不再出现底部横向滑块；铅笔位于卡片右上角。点击父来源的
  “移动”会打开文件夹选择器，按住仍可拖动，父 Skill 与全部子 Skill 一起归档。
- `karpathy--autoresearch` 这类只有 `README.md` / `program.md`、没有 `SKILL.md`
  的仓库被归为 Prompt 是正确的。展开来源后点击“准备调用”，打开完整来源目录，
  复制生成的调用内容，再粘贴到 Codex 或 Claude 的普通对话输入框。它没有
  `/karpathy--autoresearch` 或 `@karpathy--autoresearch` 命令，也不会伪装成 Skill。
- 旧版只下载 Markdown 的 Prompt 副本会显示“不完整”；删除并重新添加一次来源后，
  新版会在安全上限内保留 `train.py`、`prepare.py` 等完整项目文件，并继续执行
  逐文件安全扫描。
- MCP 连接中心现在会直接显示无法解析的配置来源，即使一个 binding 都未生成；
  Codex 插件医生的“复制诊断”会带上全部有界、脱敏证据。两者仍然只读，不会
  启动 Server、执行修复脚本或写宿主配置。

## 18. v3.1.12 父 Skill 调用、更新异常与自定义分类

- 父 Skill 的子项使用本机生成的安全绝对路径。同步结束前，软件会从已安装的
  Claude Code、Codex/ChatGPT、旧 Codex 与 Antigravity 入口逐条打开验证；任何父入口
  或子正文不可读都会明确提示未完成，并保留已有目录。
- 同一个仓库把完全相同的 Skill 同时复制到 `dist/claude`、`dist/codex` 等位置时，
  软件只显示为一个能力；内容确实不同的同名文件会标为对应宿主变体。父卡片的子项数量
  是能力数量，不是打包目录数量。
- “设置 → 未能自动更新的来源”会列出有本地改动、缺少 `.git`、超时或 Git 更新失败的
  项目。缺少 `.git` 的来源请删除后重新粘贴 GitHub 链接；有本地改动的来源先备份再决定
  是否重装。超时或 TLS 失败可先重试，不需要立即删除。
- 添加来源时，“导入后归档到”可以选择“新建自定义分类文件夹”。填写名称后，来源成功
  加入才会创建文件夹，并把父 Skill 和全部当前/未来子 Skill 一起归档。上方“自定义分类”
  文字仍用于来源的细分分类，两者用途不同。

## 19. Git 克隆、ZIP 快照与删除来源

- 添加 GitHub 来源后，安全复核页会显示实际下载方式。“Git 克隆·支持自动更新”表示正式加入后
  保留 `.git`，以后可同步创作者更新；“GitHub 快照·不支持 Git 自动更新”表示 Git 未完成，当前
  只是 ZIP/API 副本。需要自动更新时，请先点击“重试 Git 克隆”，成功后再加入。
- Windows 深层 Skill 仓库由软件自动启用 Git longpaths，不需要用户修改系统设置。仓库 hooks、脚本和
  LFS smudge 不会在导入期间执行；文件数、大小、路径和符号链接安全门禁仍然有效。
- 若 Git for Windows 的 Schannel 临时返回 `SEC_E_NO_CREDENTIALS`，软件只对本次仓库使用 Git
  自带的 OpenSSL 后端重试，不会修改全局 Git 配置。完整 Prompt 仓库会先核对 GitHub 已提交文件树的
  文件数、深度和大小，再下载工作区，避免 `blob:none` 预检长时间卡住。
- 关闭来源开关只会停止向 Claude、Codex 等工具发布，来源文件仍保留。点击“删除来源”会把受管理
  目录移到可恢复备份区，再清理配置、父路由、共享 Skills 与 Agent 链接；不会删除 GitHub 原仓库。

## 20. v3.2.0 响应速度、父 Skill 调用与 MCP 管理

### 刷新、编辑与文件夹

- “同步并刷新”同一时间只运行一份任务，不再在主刷新后串行重建整份热度快照。只读刷新期间仍可
  浏览页面；为避免两个写操作互相覆盖，移动、编辑或保存时可能短暂显示忙碌状态。
- 来源更新仍受总时间预算保护，但会记住本轮第一个延后的来源，并在下次从它开始，避免固定按字母
  排序时末尾来源长期得不到检查。损坏或缺失的轮转记录只会回退默认顺序，不会阻止同步。
- 编辑来源名称、备注、分类、启用状态和标签后，只会执行一次后台保存。返回上一级不需要等待整份
  技能库重复重载。每个更新通道的单次请求最多等待 8 秒，前台不再重复第二轮；失败会显示当前网络或通道状态。
- 六点手柄只负责拖动完整父子树；旁边的文件夹下拉只负责选择目录，不再互相抢占点击。下拉菜单和
  文件夹背景会跟随当前明暗主题。
- 每个子 Skill 后面的复制按钮会复制原作者定义的精确名称。点击复制不会展开文件夹或进入编辑页。

### Claude Code 与 Codex 中的 Skill

- 同步后，每个来源仍只发布一个小写的父 Skill 入口。子 Skill 继续通过父路由选择，并不会在
  Claude Code 或 Codex 中额外发布约 910 个全局别名。
- 复制子 Skill 名称的用途，是在选择父 Skill 后准确说明要使用哪个子能力；它不代表该子 Skill
  已经成为独立的斜杠命令。
- 新版会修复旧的断链，并正确读取合法的多行 YAML 简介。完成同步后请重启 Claude Code 或 Codex，
  让宿主重新加载技能目录；若宿主仍显示旧名称，先新建一个会话再检查。

### MCP 添加、修改与回滚

1. 在“MCP 连接中心”重扫现有 Codex 和 Claude Code 配置，选择要添加、修改或移除的服务和宿主范围。
2. 填写普通命令、参数以及所需的环境变量名或 Header 引用名。不要粘贴 Token、密码或 API Key
   的实际值；明显的凭据参数与常见 Token 前缀会被前后端拒绝，凭据只能通过变量名引用。
3. 先查看脱敏字段差异，再明确确认。软件会原子写入配置，并立即静态回读；多宿主操作中任一目标
   失败时，已经写入的目标会一起恢复，不留下半套配置。确认期间的普通启动字段只保留在当前进程
   内；磁盘计划只记录经当前 Windows 用户加密的目标指纹和脱敏差异。未确认计划 15 分钟后失效，内存最多保留
   64 份或 8 MiB 请求数据；执行成功或失败都会消费本次计划，需再次修改时重新预览。
4. 成功后可使用本次快照一键回滚；重启 AI SkillHub 后仍可看到未过期快照。回滚内容仅保存在
   AI SkillHub 私有状态目录，Windows 下由当前用户的 DPAPI 保护，最多保留 7 天、16 份或 128 MiB，
   不会在宿主配置旁生成备份。若其它程序在计划后或应用后改过目标文件，应用/回滚都会拒绝覆盖并
   要求重新预览。含注释或尾逗号的 Claude JSONC 文件仍可盘点，但保持只读，请使用 Claude Code
   自己的配置方式处理。
5. Codex Binding 可在 AI SkillHub 中启停；Claude Code 的启停是按项目保存的宿主状态，请在
   Claude Code 的 `/mcp` 面板中操作。AI SkillHub 不会写一个 Claude 不识别的 `disabled` 假字段。
6. AI SkillHub 不会启动 MCP Server，也不会把“配置写入成功”冒充“服务已运行”。修改后重启对应
   宿主，再在 Codex 或 Claude Code 中输入 `/mcp`，确认服务能够真正启动和响应。
